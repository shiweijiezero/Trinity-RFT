# 审稿人 TC9w（中文翻译）

## 评分

- 置信度：4（相当确定）
- 可靠性（Soundness）：3.5
- 兴奋度（Excitement）：4（令人兴奋）
- 总体评价：4（Conference）
- 可复现性：5
- 数据集：1
- 软件：4

---

## 论文总结

本文提出 R³L（Reflect-then-Retry Reinforcement Learning，反思后重试强化学习），用于在多轮智能体任务和数学推理任务中对大语言模型进行强化学习后训练。该方法针对作者所指出的 GRPO 类训练方法的三种失效模式：

1. 在困难、稀疏奖励任务上，随机探索效率低下；
2. 轨迹级信用分配会在后续步骤出错时一并惩罚原本正确的前缀；
3. 在失败样本占主导的组中，破坏性梯度（抑制错误的梯度）会压过少量建设性梯度，从而导致熵坍塌。

R³L 包含三个组成部分：

1. **语言引导的 Reflect-then-Retry。** 一半 rollout 预算用于基础采样。每条失败或低效的基础轨迹都会得到一份结构化 JSON 反思，其中包括结果分类、根因、改进建议和 pivot turn；随后，从 pivot 位置开始，在 guidance 条件下重新生成修正后的后缀。
2. **Pivotal Credit Assignment。** 使用二值 mask 将 pivot 之前所有 turn 的梯度置零，使更新集中在发生分歧的后缀；作者从控制变量降低方差的角度为此提供了动机。
3. **Positive Amplification。** 在组相对归一化之后，将正 advantage 按系数 α 放大（α=3），最大回报轨迹设为 α，负 advantage 保持不变。同时完全移除 importance sampling 和 KL。

贡献类型：主要是一篇方法/算法论文，并包含大量实证分析。

## 优点总结

1. **问题拆解清晰，设计与问题一一对应。** C1–C3 表述明确，每个问题都对应一个组件，因此方法本身以及表 2 的消融实验都容易理解。
2. **实证支持充分。** 实验和消融结果较为全面，并表明 Reflect-then-Retry 是贡献最大的组件。
3. **有具体的定性机制解释。** ALFWorld/DAPO 案例研究以及 pivot 演化分析展示了反思在机制层面具体改变了什么。
4. **可复现性完整。** 论文提供了完整 prompt、超参数表、数据集统计、各 baseline 的具体设置以及匿名代码链接。

## 缺点总结

1. 论文从理论上解释了为何移除 importance sampling 和 KL，但没有通过重新保留这些机制的消融实验进行比较。
2. 当前方法只在小规模模型上进行了测试，审稿人希望了解在大规模模型和强 baseline 上的表现。

## 评论、建议与拼写问题

无。

## 伦理问题

本投稿不存在伦理方面的担忧。

是否需要伦理审查：否。

## 其他字段

- 是否知道或推测作者身份：否。
- 是否从外部来源了解本文：不适用；审稿人未从外部来源了解本文。
- 是否知道论文来源：不适用；审稿人未从外部来源了解论文来源。
- 上述知识是否影响评审：不适用；审稿人未从外部来源了解本文。
- 审稿人认证：审稿人确认其评审准确反映了本人对该工作的评价。若使用了任何自动化工具，其用途仅限于改善语法和文风，评审实质内容来自审稿人本人或已注明的第二审稿人。
- 出版伦理政策合规：审稿人仅在 PEC 政策允许的场景中使用了保护隐私的工具，例如语言编辑。

## 回复草稿

感谢审稿人的积极评价和建设性建议。

1. **关于 KL 与 importance sampling 的消融**

关于 KL 与 importance sampling，我们补充了 Qwen2.5-1.5B-Instruct 在 ALFWorld 上的四组对照实验：R³L、R³L+KL、R³L+IS 和 R³L+KL+IS，最终结果分别为 **[R3L结果]、[R3L+KL结果]、[R3L+IS结果]、[R3L+KL+IS结果]**。我们想强调，R³L 的机制能够提高训练稳定性，并在该设置下不依赖 KL 和 IS 也能稳定训练。这并不意味着 KL 和 IS 不重要；二者仍然是通用且有效的稳定训练手段，也与 R³L 完全兼容。当训练本身没有发生不稳定或崩溃时，加入或不加入它们的性能差异 **[根据实验结果填写]**。我们会在论文中明确这一结论的适用范围。

2. **关于模型规模与更强 baseline**

关于模型规模，我们完全认同在更大、更强的模型上进一步验证 R³L 的必要性。受限于当前计算资源和较短的 rebuttal 周期，我们暂时无法在几天内可靠地完成这类训练。更新模型还会带来额外的基础设施要求。例如，Qwen3.5 采用 Gated DeltaNet 与 Gated Attention 结合的 hybrid architecture，目前 RL 训练基础设施对这类新架构的 kernel、分布式训练和稳定性支持仍不够成熟，需要经过谨慎验证后才能进行可信的比较。当前实验已覆盖 Qwen2.5-1.5B-Instruct、Qwen2.5-7B-Instruct、更新的 Qwen3-4B，以及跨架构的 Llama-3.2-3B-Instruct，并在三个智能体环境和六个数学 benchmark 上与 GSPO、Critique-GRPO 等强 baseline 进行了比较。

此外，在论文提交之后，我们注意到一项独立的后续工作 [Agent Reinforcement Learning via Pivotal-Aware Self-Feedback Retry](https://arxiv.org/abs/2607.03702)（PivoARL，2026 年 7 月发布）。该工作同样通过结构化反思定位 pivotal error、从 pivot state 进行局部 retry、复用正确前缀，并采用 pivotal-aware credit assignment；其在 4 个智能体任务和 7 个搜索问答 benchmark 上报告了一致提升。虽然这不能替代我们自己的大规模模型实验，但这一独立工作对相近设计原则得到的结果，为 R³L 的核心思想提供了补充性的外部证据。
